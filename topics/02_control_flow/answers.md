# Topic 2 Answers

1. `if rows_loaded == 0: print("No rows loaded")`
2. `status = "valid" if amount >= 0 else "invalid"`
3. `for file_name in file_names: print(file_name)`
4. `for number in range(1, 6): print(number)`
5. `if value < 0: continue`
6. `if item == "STOP": break`
7. `doubled = [number * 2 for number in [1, 2, 3, 4]]`
8. `evens = [number for number in range(1, 11) if number % 2 == 0]`
9. `count = 1` then loop while `count <= 3`
10. Increase a counter when `row["is_valid"]` is true
11. `label = "high_value" if total > 1000 else "standard"`
12. `if row["country"] == "USA": print(row)`
