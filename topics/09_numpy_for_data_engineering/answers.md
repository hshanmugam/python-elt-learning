# Topic 9 Answers

1. `import numpy as np`
2. `values = np.array([10, 20, 30, 40])`
3. `values + 5`
4. `values * 2`
5. `np.sum(values)`
6. `np.mean(values)`
7. `mask = values > 100`
8. `filtered = values[mask]`
9. `matrix = np.array([1, 2, 3, 4, 5, 6]).reshape(2, 3)`
10. `np.max(values)`
11. Vectorized operations are typically faster and easier to read for column-wide numeric logic.
12. They are useful when standardizing many sensor measurements or transaction amounts before loading.
