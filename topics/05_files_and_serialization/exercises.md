# Topic 5 Exercises

1. Import the `csv` module.
2. Import the `json` module.
3. Use `Path` from `pathlib` to define a file path.
4. Write text to a file called `log.txt`.
5. Read a text file and print its content.
6. Write a list of dictionaries to a CSV file.
7. Read a CSV file with `csv.DictReader`.
8. Convert a Python dictionary to JSON text using `json.dumps`.
9. Write a Python dictionary to a JSON file using `json.dump`.
10. Read JSON from a file using `json.load`.
11. Create a path for `data/raw/customers.csv` using `Path`.
12. Explain why `with open(...)` is safer than plain `open(...)`.
