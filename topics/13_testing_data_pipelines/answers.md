# Topic 13 Answers

1. A unit test checks that one small piece of code behaves as expected.
2. `assert 2 + 2 == 4`
3. Edge cases matter because pipeline bugs often appear with nulls, blanks, or unusual values.
4. Verify that a missing country becomes a default like `"USA"` or is flagged correctly.
5. Verify that repeated business keys are identified or removed according to the rule.
6. A fixture provides reusable setup data or objects for tests.
7. `pytest` offers concise syntax and good test discovery.
8. Tests reduce the risk of deploying a broken recurring job.
9. Confirm that `-5` is rejected while `5` is accepted.
10. Code testing checks program logic, while data quality validation checks the dataset produced or consumed by the logic.
11. Verify that grouped totals match expected customer-level sums.
12. Tests help refactoring because they show whether behavior changed unexpectedly.
