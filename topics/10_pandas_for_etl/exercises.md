# Topic 10 Exercises

1. Import pandas as `pd`.
2. Create a DataFrame with columns `customer_id` and `amount`.
3. Select one column from a DataFrame.
4. Filter rows where `amount > 100`.
5. Create a new column called `tax` as `amount * 0.1`.
6. Rename a column from `amount` to `order_amount`.
7. Count how many null values appear in each column.
8. Fill missing values in a `country` column with `"USA"`.
9. Group by `customer_id` and sum `amount`.
10. Merge an orders DataFrame with a customers DataFrame on `customer_id`.
11. Explain why DataFrames are useful in ETL work.
12. Describe one case where pandas is better than manual Python loops.
